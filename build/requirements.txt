# Resume/cover-letter rendering dependencies (pure-Python; no system libs needed)
python-docx>=1.1     # native Word (.docx) output — for portals that request or favor Word
reportlab>=4.0       # polished, text-based PDF with exact pagination (page/readability QA)
pymupdf>=1.24        # score-meter gauge (score_meter.py) + PDF preview/verify
